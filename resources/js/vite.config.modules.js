import { createModuleViteConfig } from './plugins/moduleResolver.js';

// Laravel-specific configuration
export default createModuleViteConfig({
    plugins: [
        // Laravel Vite plugin will be added here
    ]
});
